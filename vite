#!/bin/bash

# Install vite if missing
if ! command -v npx &> /dev/null; then
    npm install -g npx
fi

if [ ! -f "node_modules/.bin/vite" ]; then
    npm install vite
fi

# Run vite directly
exec npx vite "$@"