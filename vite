#!/bin/bash
# Simple and reliable vite startup script

# Add node_modules/.bin to PATH
export PATH="./node_modules/.bin:$PATH"

# Try running vite directly
if command -v vite >/dev/null 2>&1; then
    exec vite "$@"
else
    # Fallback to npx
    exec npx vite "$@"
fi