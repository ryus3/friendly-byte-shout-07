{
  "name": "inventory-management-system",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "./npm-install-vite.sh || npx vite --host :: --port 8080",
    "build": "npx vite build", 
    "preview": "npx vite preview"
  }
}