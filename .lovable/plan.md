أفهم غضبك. فحصت فعلياً قاعدة البيانات والكود الحالي، وليس افتراضاً. النتيجة الواضحة:

1. حساب `alshmry94` غير محذوف وغير معطل:
   - `is_active = true`
   - `auto_renew_enabled = true`
   - `expires_at` صالح إلى 2026-05-04
   - المشكلة ليست حذف الحساب من قاعدة البيانات.

2. الفاتورة `3271348` موجودة في قاعدة البيانات:
   - `orders_count = 5`
   - `amount = 95,000`
   - `received = true`
   - لكن جدول طلبات الفاتورة `delivery_invoice_orders` فيه `0` صفوف لهذه الفاتورة، لذلك النافذة تعرض: لا توجد طلبات.

3. سبب عدم جلب طلبات الفاتورة الآن واضح في الكود:
   - في `useAlWaseetInvoices.js` تم اعتبار الفاتورة المستلمة `received=true` كـ `cacheOnly`.
   - هذا يمنع جلب طلبات الفاتورة من شركة التوصيل إذا كان الكاش المحلي فارغاً.
   - لذلك فاتورة مثل `3271348`، مستلمة لكن تفاصيل طلباتها غير محفوظة، تبقى فارغة ولا تحاول الجلب من API.

4. رقم المخزون المحجوز 4 مصدره خطأ UI وليس قاعدة البيانات:
   - قاعدة `inventory.reserved_quantity` مجموعها الآن `0`.
   - نافذة المخزون المحجوز تعرض `0` وهذا الصحيح.
   - الكارت العلوي يحسب fallback من طلبين حالتهما `delivery_status = 17`، بكميات 3 و1، فيظهر 4.
   - وهذا مخالف للقاعدة الذهبية: `delivery_status 4 أو 17` لا يحجز أبداً.

الخطة بعد موافقتك:

1. إصلاح طلبات الفاتورة جذرياً
   - تعديل `src/hooks/useAlWaseetInvoices.js` بحيث لا يمنع `received=true` جلب طلبات الفاتورة إذا كان `delivery_invoice_orders` فارغاً والعداد `orders_count > 0`.
   - السلوك الصحيح:
     - إذا الكاش يحتوي طلبات: اعرضها من القاعدة.
     - إذا الكاش فارغ والفاتورة تقول عندها طلبات: اجلب من شركة التوصيل بالتوكن الصحيح لصاحب الفاتورة.
     - بعدها احفظ الطلبات في `delivery_invoice_orders` واربطها بالطلبات المحلية.

2. إصلاح شكل استجابة `getInvoiceOrders`
   - في `src/lib/alwaseet-api.js` سأجعل دالة `getInvoiceOrders` تتعامل مع كل الأشكال المتوقعة من الوسيط:
     - `data.orders`
     - مصفوفة مباشرة
     - أي كائن يحتوي `orders`
   - وتعيد دائماً شكلاً موحداً: `{ orders: [...] }` حتى لا يفشل الكود بسبب اختلاف شكل الرد.

3. إصلاح المزامنة الخلفية للفواتير
   - في `supabase/functions/smart-invoice-sync/index.ts` سأصحح جلب طلبات الفاتورة ليحلل جسم الرد حتى لو رجع HTTP غير 200 مع JSON، ويستخرج `data.orders` بشكل صحيح.
   - لن يتم تعطيل أي توكن بسبب خطأ فواتير.
   - لن يتم حذف كاش الفواتير عند فشل API أو رجوع نتيجة فارغة.

4. ترميم الفواتير الموجودة حالياً بدون ترقيع مالي
   - إضافة/استخدام self-healing آمن: عند فتح فاتورة موجودة وعدد طلباتها أكبر من صفر لكن روابطها فارغة، يحاول:
     1. جلب طلباتها من API بالتوكن الصحيح لصاحبها.
     2. حفظها في `delivery_invoice_orders`.
     3. تشغيل `link_invoice_orders_to_orders` لربطها بالطلبات المحلية.
   - لن أغير أرصدة أو أرباح أو مخزون يدوياً؛ فقط روابط الفاتورة ولقطة طلبات شركة التوصيل.

5. إصلاح رقم المخزون المحجوز في الكارت
   - تعديل `src/hooks/useInventoryStats.js` ليحسب المحجوز من مصدر الحقيقة فقط: `inventory.reserved_quantity` أو من الطلبات مع استثناء صريح لـ `delivery_status 4 و17`.
   - حالياً القاعدة تقول 0، لذلك الكارت سيصبح 0 مثل النافذة.
   - سأراجع `ReservedStockDialog.jsx` للتأكد أن نفس القاعدة مطبقة في كل مكان.

6. منع تسجيل خروج الوسيط الكاذب
   - إزالة/تعديل منطق تسجيل الخروج الصامت في فحص انتهاء التوكن داخل `AlWaseetContext.jsx` بحيث لا يمسح الجلسة المحلية عند فشل تجديد عابر أو خطأ endpoint.
   - توكن صالح في DB يبقى هو مصدر الحقيقة.
   - `errNum:21` للفواتير أو الحالات لا يساوي انتهاء جلسة ولا يعطل الحساب.

7. التحقق بعد الإصلاح
   - فحص قاعدة البيانات للفواتير:
     - `3271348` يجب أن يصبح لها rows في `delivery_invoice_orders` إن رجعها API أو تتربط من البيانات المحلية إن وجدت.
     - الفاتورة `3247172` تبقى كما هي ولا تتضرر.
   - فحص الحسابات:
     - `alshmry94` يبقى active وصالح.
     - `ryusiq` يبقى كما هو.
   - فحص الجرد:
     - مجموع `inventory.reserved_quantity = 0`.
     - كارت مخزون محجوز = 0.
     - نافذة المخزون المحجوز = 0.

ملفات التنفيذ المتوقعة:
- `src/hooks/useAlWaseetInvoices.js`
- `src/lib/alwaseet-api.js`
- `supabase/functions/smart-invoice-sync/index.ts`
- `src/hooks/useInventoryStats.js`
- `src/contexts/AlWaseetContext.jsx`
- احتمال Migration صغيرة فقط إذا احتجنا RPC آمن لملء روابط الفواتير من الطلبات المحلية أو تحسين self-healing.

لن أعمل revert أعمى ولن ألمس الحسابات أو أحذف بيانات. الإصلاح سيكون على سبب الخلل نفسه: منع جلب طلبات الفاتورة بسبب `cacheOnly`، وحساب المحجوز من طلبات status=17 رغم أن قاعدة المشروع تمنع ذلك.