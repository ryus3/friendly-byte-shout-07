

# خطة: إصلاح عرض فواتير شركات التوصيل الموحد

## المشكلة الفعلية المكتشفة

### 1. التغييرات لم تظهر للمستخدم
| الملف | في الكود الحالي | ما يراه المستخدم |
|-------|----------------|-----------------|
| `AlWaseetInvoicesTab.jsx` السطر 349 | `فواتير شركات التوصيل` | `فواتير الوسيط` ❌ |

**السبب المحتمل:** Build لم يتم بشكل صحيح أو Cache قديم.

### 2. لا توجد فواتير مدن في قاعدة البيانات
| الشركة | عدد الفواتير | التوكنات النشطة |
|--------|-------------|----------------|
| الوسيط | 341 | 1 |
| مدن | **0** | 1 |

**السبب:** توكن مدن جديد ولم تتم مزامنة الفواتير بعد.

---

## الحل المقترح (خطوتان)

### الخطوة 1: فرض إعادة البناء والتحقق من الملفات
سأعدل الملف بطريقة تضمن تحديث الكود:

```jsx
// في AlWaseetInvoicesTab.jsx - إضافة console.log للتحقق
console.log('🔥 النسخة الجديدة: فواتير موحدة لجميع الشركات');
```

### الخطوة 2: مزامنة فواتير مدن يدوياً
استدعاء Edge Function لمزامنة فواتير مدن:

```typescript
await supabase.functions.invoke('smart-invoice-sync', {
  body: { 
    mode: 'comprehensive',
    sync_invoices: true,
    force_refresh: true
  }
});
```

---

## التحقق بعد الإصلاح

1. التأكد من ظهور **"فواتير شركات التوصيل"** كعنوان
2. التأكد من وجود **Badge "الوسيط + مدن"**
3. التأكد من وجود **فلتر الشركة** (جميع الشركات / الوسيط / مدن)
4. التأكد من ظهور **فواتير مدن** (إن وجدت في API)

---

## ملخص التعديلات

| الملف | التعديل | السبب |
|-------|---------|-------|
| `AlWaseetInvoicesTab.jsx` | إضافة تعليق/console.log للتحقق | فرض Rebuild |
| `smart-invoice-sync` | استدعاء يدوي | جلب فواتير مدن |

