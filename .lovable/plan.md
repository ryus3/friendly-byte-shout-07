
# خطة: عرض فواتير جميع شركات التوصيل بشكل موحد

## المشكلة الحالية

| المشكلة | السبب |
|---------|-------|
| عرض فواتير شركة واحدة فقط | الفلترة تعتمد على `activePartner` الحالي فقط |
| لا يوجد badge لاسم الشركة | التصميم الحالي يفترض شركة واحدة |
| العنوان يتغير حسب الشركة | "فواتير الوسيط" أو "فواتير مدن" بدلاً من "فواتير شركات التوصيل" |

---

## الحل العالمي المقترح

### 1. تحديث Hook الفواتير (`useAlWaseetInvoices.js`)

**التعديل الرئيسي:** جلب فواتير **جميع الشركات** بدلاً من الشركة النشطة فقط.

```javascript
// قبل (السطر ~246):
.eq('partner', activePartner)

// بعد:
.in('partner', ['alwaseet', 'modon'])
```

### 2. تحديث مكون القائمة (`AlWaseetInvoicesList.jsx`)

**إضافة Badge جميل لاسم شركة التوصيل:**

```jsx
// إضافة Badge ملون لكل شركة
<Badge 
  className={`${
    invoice.partner === 'modon' 
      ? 'bg-gradient-to-r from-cyan-500 to-blue-600' 
      : 'bg-gradient-to-r from-orange-500 to-red-600'
  } text-white`}
>
  {invoice.partner === 'modon' ? 'مدن' : 'الوسيط'}
</Badge>
```

### 3. تحديث تبويب الفواتير (`AlWaseetInvoicesTab.jsx`)

**تغييرات:**
1. العنوان: من "فواتير الوسيط" إلى "فواتير شركات التوصيل"
2. إزالة شرط `activePartner` الحصري
3. إضافة فلتر للشركة (اختياري)

### 4. تحديث فلتر الحسابات (`accountFilter`)

**إضافة فلتر "شركة التوصيل":**
- الكل
- الوسيط
- مدن

---

## التعديلات التفصيلية

### الملف 1: `src/hooks/useAlWaseetInvoices.js`

| السطر | التعديل |
|-------|---------|
| ~22 | إزالة شرط `activePartner` من `fetchInvoices` |
| ~35-45 | جلب التوكنات لـ **كلا الشركتين** |
| ~246 | `.in('partner', ['alwaseet', 'modon'])` بدلاً من `.eq('partner', activePartner)` |

### الملف 2: `src/components/orders/AlWaseetInvoicesTab.jsx`

| السطر | التعديل |
|-------|---------|
| ~143-155 | إزالة شرط `activePartner` أو تعديله للسماح بعرض البيانات |
| ~354 | تغيير العنوان إلى "فواتير شركات التوصيل" |
| ~360-374 | عرض الحسابات مجمعة مع اسم الشركة |

### الملف 3: `src/components/orders/AlWaseetInvoicesList.jsx`

| السطر | التعديل |
|-------|---------|
| ~166-205 | تحسين Header لعرض اسم الشركة بـ Badge ملون أكثر وضوحاً |

---

## التصميم المقترح لكارت الفاتورة

```text
┌─────────────────────────────────────────┐
│  فاتورة #2782552           [مُستلمة] ✓  │
│                                          │
│  ┌──────┐  الوسيط                        │
│  │  🏢  │  ALSHMRY94              #1234  │
│  └──────┘                                │
│  ──────────────────────────────────────  │
│  💰 445,000 د.ع                          │
│  📦 15 طلب مُسلم                         │
│  📅 منذ يوم واحد                         │
│                                          │
│  [═══════ عرض التفاصيل ═══════]         │
└─────────────────────────────────────────┘
```

**مع Badge الشركة الملون:**
```text
- الوسيط: خلفية برتقالية-حمراء gradient
- مدن: خلفية سماوية-زرقاء gradient
```

---

## إضافة فلتر الشركة (اختياري)

```jsx
<Select value={partnerFilter} onValueChange={setPartnerFilter}>
  <SelectTrigger>
    <Building className="h-4 w-4 mr-2" />
    <SelectValue placeholder="الشركة" />
  </SelectTrigger>
  <SelectContent>
    <SelectItem value="all">جميع الشركات</SelectItem>
    <SelectItem value="alwaseet">الوسيط فقط</SelectItem>
    <SelectItem value="modon">مدن فقط</SelectItem>
  </SelectContent>
</Select>
```

---

## التأثير على الأداء

| الجانب | قبل | بعد |
|--------|-----|-----|
| عدد الاستعلامات | 1 (شركة واحدة) | 1 (كلا الشركتين) |
| حجم البيانات | متوسط | نفسه (مع pagination) |
| تجربة المستخدم | تبديل بين الشركات | رؤية شاملة |

---

## ملخص الملفات المطلوب تعديلها

| الملف | التعديل |
|-------|---------|
| `src/hooks/useAlWaseetInvoices.js` | جلب فواتير كل الشركات |
| `src/components/orders/AlWaseetInvoicesTab.jsx` | عنوان موحد + فلتر الشركة |
| `src/components/orders/AlWaseetInvoicesList.jsx` | Badge ملون للشركة |

---

## الجواب على سؤالك الأول

### ✅ نعم، حساب الخصم والزيادة سيعمل لكلا الشركتين!

**الكود المحدث في `sync-order-updates/index.ts`:**
- يدعم كلا الشركتين (`alwaseet` و `modon`)
- يحسب `discount` و `price_increase` و `price_change_type`
- يستخدم `priceNeedsRecalculation` للطلبات القديمة
- الوسيط **لم يتأثر** سلبياً
